[gd_scene load_steps=2 format=3]

; Bootstrap scene. Instances the combat sandbox on ready — see Main.cs.
; This stays the stable entry point; it will route to the Workshop instead
; of straight to combat once Milestone 4 exists.

[ext_resource type="Script" path="res://Scenes/Main.cs" id="1"]

[node name="Main" type="Node2D"]
script = ExtResource("1")
