[gd_scene load_steps=2 format=3]

; Bootstrap scene. Instances the expedition map on ready — see Main.cs.
; This stays the stable entry point; it will route to the Workshop first
; instead, once that exists (Milestone 4 issue 4.6).

[ext_resource type="Script" path="res://Scenes/Main.cs" id="1"]

[node name="Main" type="Node2D"]
script = ExtResource("1")
