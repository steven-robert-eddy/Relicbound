# Artifact content

One JSON file per artifact: `id`, `name`, `tags`, `trigger`, `effects`.

> Adding an artifact means adding a file here. If it needs a code change, fix the system instead.

See [`docs/CODING_STANDARDS.md`](../../../docs/CODING_STANDARDS.md) for the rules that govern this folder.
