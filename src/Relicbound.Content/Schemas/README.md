# Content schemas

JSON schemas describing each content type, plus the validation rules applied at load.

> Content is validated once at startup and reports every failure at once. A content error is a build error, not a runtime mystery.

See [`docs/CODING_STANDARDS.md`](../../../docs/CODING_STANDARDS.md) for the rules that govern this folder.
