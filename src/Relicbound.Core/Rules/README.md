# Rules

Turn structure, the action-cost and resource model (`ITurnResourceModel`, `ActionPointModel`), grid rules, and the seeded random source.

> The action economy lives behind an abstraction so 3 AP can become something else without rewriting the systems built on it.

See [`docs/CODING_STANDARDS.md`](../../../docs/CODING_STANDARDS.md) for the rules that govern this folder.
