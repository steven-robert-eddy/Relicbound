namespace Relicbound.Core.Entities;

// Grows as content defines more statuses (Milestone 2). Burn is named here
// because it already appears in docs/GAME_DESIGN.md's canonical Ember Heart
// example.
public enum StatusType
{
    Burn
}
